<h1>Endpoints Overview:</h1>

<% @endpoints.each do |endpoint| %>
  
  <ul>
    <li>
      <b><a href="/mock/<%= endpoint[0] %>">/mock/<%= endpoint[0] %></a>:</b>
      <ul>
        <li><b>file:</b> <%= endpoint[1]["file"] %></li>
      </ul>
    </li>
  </ul>
  
<% end %>
